--- a/src/index.css
+++ b/src/index.css
@@ -66,3 +66,872 @@
 .animate-slide-in-right {
   animation: slide-in-right 0.25s ease-out;
 }
+
+/* Shared workspace surfaces keep navigation separate from the reading canvas. */
+@theme {
+  --font-sans:'Segoe UI','Noto Sans Bengali',system-ui,sans-serif;
+}
+:root {
+  color:#282d35;
+  background:#fafbfc
+}
+button:not(:disabled),summary {
+  cursor:pointer
+}
+button:disabled {
+  opacity:.45;
+  cursor:not-allowed
+}
+.app-shell {
+  height:100dvh;
+  display:flex;
+  flex-direction:column
+}
+.workspace-header {
+  height:70px;
+  flex-shrink:0;
+  display:flex;
+  align-items:center;
+  gap:22px;
+  padding:0 26px;
+  border-bottom:1px solid #e8e9ec;
+  background:white;
+  position:relative;
+  z-index:30
+}
+.workspace-brand {
+  display:flex;
+  align-items:center;
+  gap:10px;
+  color:#24272c;
+  text-decoration:none;
+  white-space:nowrap
+}
+.workspace-brand strong {
+  font-size:17px;
+  letter-spacing:-.6px
+}
+.workspace-brand strong span {
+  font-weight:400;
+  margin-left:4px
+}
+.brand-symbol {
+  background:#e83330;
+  color:white;
+  display:grid;
+  place-items:center;
+  width:31px;
+  height:31px;
+  border-radius:9px
+}
+.workspace-divider {
+  height:22px;
+  width:1px;
+  background:#e4e5e8
+}
+.workspace-name {
+  font-size:13px;
+  color:#686e78;
+  max-width:170px;
+  overflow:hidden;
+  text-overflow:ellipsis;
+  white-space:nowrap
+}
+.workspace-nav {
+  display:flex;
+  gap:6px;
+  margin-left:auto
+}
+.workspace-nav button {
+  display:flex;
+  align-items:center;
+  gap:7px;
+  padding:9px 12px;
+  border-radius:8px;
+  color:#666c77;
+  font-size:13px
+}
+.workspace-nav button[aria-current],.workspace-nav button:hover {
+  background:#f4f5f7;
+  color:#24272c
+}
+.account-menu {
+  position:relative
+}
+.account-menu summary {
+  display:flex;
+  align-items:center;
+  gap:10px;
+  list-style:none
+}
+.account-menu summary::-webkit-details-marker {
+  display:none
+}
+.account-avatar {
+  display:grid;
+  place-items:center;
+  width:34px;
+  height:34px;
+  border-radius:50%;
+  background:#f4ebe8;
+  color:#924a3c;
+  font-size:13px;
+  font-weight:600
+}
+.account-name {
+  font-size:12px;
+  font-weight:600
+}
+.account-name small {
+  display:block;
+  color:#838793;
+  font-weight:400;
+  font-size:11px;
+  text-transform:capitalize
+}
+.account-popover {
+  position:absolute;
+  right:0;
+  top:48px;
+  padding:8px;
+  width:240px;
+  background:white;
+  border:1px solid #e5e7eb;
+  border-radius:12px;
+  box-shadow:0 12px 40px #171c2820
+}
+.account-popover p {
+  font-size:12px;
+  color:#777d87;
+  padding:10px;
+  overflow-wrap:anywhere
+}
+.account-popover button {
+  display:flex;
+  align-items:center;
+  gap:10px;
+  font-size:13px;
+  width:100%;
+  padding:10px;
+  border-radius:7px
+}
+.account-popover button:hover {
+  background:#f4f5f7
+}
+.coach-layout {
+  display:flex;
+  height:100%;
+  overflow:hidden;
+  position:relative
+}
+.coach-sidebar {
+  width:264px;
+  flex-shrink:0;
+  display:flex;
+  flex-direction:column;
+  background:#22252b;
+  color:#d3d6dc;
+  min-height:0;
+  padding:22px 14px 16px
+}
+.sidebar-heading {
+  padding:0 10px 24px
+}
+.sidebar-coach-icon {
+  display:grid;
+  place-items:center;
+  width:36px;
+  height:36px;
+  background:#363940;
+  border:1px solid #4a4c52;
+  border-radius:10px
+}
+.sidebar-title {
+  font-size:15px;
+  font-weight:600;
+  color:white
+}
+.sidebar-heading .text-gray-500 {
+  color:#aeb3bd;
+  font-size:11px;
+  margin-top:4px
+}
+.sidebar-modes {
+  display:flex;
+  flex-direction:column;
+  gap:3px
+}
+.mode-button {
+  display:flex;
+  gap:12px;
+  align-items:center;
+  padding:10px 12px;
+  border-radius:8px;
+  text-align:left;
+  color:#b7bdc8
+}
+.mode-button:hover {
+  background:#2d3037;
+  color:white
+}
+.mode-button.is-active {
+  background:#393b43;
+  color:white
+}
+.mode-button.is-active>svg {
+  color:#ff857d
+}
+.mode-button .text-gray-400 {
+  font-size:11px;
+  color:#9299a6;
+  margin-top:2px
+}
+.sidebar-history {
+  min-height:80px;
+  flex:1;
+  display:flex;
+  flex-direction:column;
+  margin-top:28px;
+  overflow:hidden
+}
+.history-heading {
+  display:flex;
+  justify-content:space-between;
+  align-items:center;
+  padding:0 12px 12px
+}
+.history-heading h2 {
+  font-size:12px;
+  font-weight:600;
+  color:#959da9
+}
+.history-heading button {
+  padding:4px;
+  color:#afb6c2
+}
+.history-search {
+  display:flex;
+  align-items:center;
+  gap:8px;
+  padding:9px 10px;
+  background:#2a2d34;
+  border:1px solid #363942;
+  border-radius:7px;
+  color:#7f8998;
+  margin:0 5px 8px
+}
+.history-search input {
+  min-width:0;
+  width:100%;
+  font-size:12px;
+  background:transparent;
+  outline:none;
+  color:#e2e5eb
+}
+.history-list {
+  overflow-y:auto;
+  padding:0 3px;
+  scrollbar-width:thin;
+  scrollbar-color:#515560 transparent
+}
+.history-list>button {
+  display:block;
+  width:100%;
+  text-align:left;
+  padding:10px;
+  border-radius:7px;
+  font-size:13px;
+  margin:3px 0;
+  color:#bec4ce
+}
+.history-list>button:hover,.history-list>button.is-active {
+  background:#33363e;
+  color:white
+}
+.history-list>button.is-active {
+  box-shadow:inset 2px 0 #ef635b
+}
+.history-list>button span {
+  display:block;
+  overflow:hidden;
+  text-overflow:ellipsis;
+  white-space:nowrap
+}
+.history-list small {
+  display:block;
+  font-size:10px;
+  color:#8d96a5;
+  margin-top:5px
+}
+.history-list p {
+  font-size:12px;
+  line-height:1.6;
+  color:#939caa;
+  padding:12px
+}
+.sidebar-footer {
+  padding-top:16px;
+  margin-top:12px;
+  border-top:1px solid #3a3e46
+}
+.new-conversation-button {
+  display:flex;
+  align-items:center;
+  justify-content:center;
+  gap:8px;
+  background:#e83330;
+  color:white;
+  padding:11px;
+  border-radius:8px;
+  width:100%;
+  font-size:12px;
+  font-weight:600;
+  margin-bottom:12px
+}
+.new-conversation-button:hover {
+  background:#cd2926
+}
+.coach-main {
+  display:flex;
+  flex-direction:column;
+  flex:1;
+  min-width:0;
+  min-height:0;
+  background:#fafbfc
+}
+.conversation-toolbar {
+  display:flex;
+  flex-wrap:wrap;
+  align-items:center;
+  gap:18px;
+  padding:18px 30px;
+  background:white;
+  border-bottom:1px solid #eceef1;
+  min-height:78px
+}
+.conversation-title {
+  min-width:0;
+  flex:1
+}
+.conversation-title strong {
+  font-size:14px;
+  font-weight:600;
+  display:block;
+  max-width:560px;
+  white-space:nowrap;
+  overflow:hidden;
+  text-overflow:ellipsis
+}
+.conversation-title>span {
+  display:block;
+  font-size:11px;
+  color:#89919d;
+  margin-top:4px
+}
+.save-status {
+  display:flex;
+  align-items:center;
+  gap:4px;
+  font-size:11px;
+  color:#78828d
+}
+.toolbar-new {
+  display:flex;
+  align-items:center;
+  gap:7px;
+  border:1px solid #e3e6eb;
+  border-radius:7px;
+  padding:8px 10px;
+  color:#59616d;
+  font-size:12px
+}
+.mobile-sidebar-toggle {
+  display:none
+}
+.coach-welcome {
+  flex:1;
+  min-height:0;
+  overflow-y:auto;
+  text-align:center;
+  padding:clamp(28px,6vh,70px) 32px 24px
+}
+.welcome-mark {
+  display:grid;
+  place-items:center;
+  width:54px;
+  height:54px;
+  border-radius:16px;
+  background:#fff0ed;
+  color:#e83330;
+  margin:0 auto 24px
+}
+.welcome-kicker {
+  font-size:12px;
+  color:#888f9a;
+  margin-bottom:12px
+}
+.coach-welcome h1 {
+  font-size:clamp(28px,3vw,40px);
+  line-height:1.2;
+  letter-spacing:-1.2px;
+  font-weight:600;
+  color:#24272d
+}
+.welcome-description {
+  font-size:14px;
+  color:#7a828e;
+  line-height:1.8;
+  margin-top:18px
+}
+.welcome-suggestions {
+  display:grid;
+  grid-template-columns:repeat(3,1fr);
+  gap:14px;
+  max-width:730px;
+  margin:34px auto 26px
+}
+.welcome-suggestions button {
+  position:relative;
+  text-align:left;
+  border:1px solid #e4e7ec;
+  border-radius:12px;
+  background:white;
+  padding:21px 18px;
+  transition:border-color .15s,background .15s;
+  color:#64707d
+}
+.welcome-suggestions button:hover {
+  border-color:#e79a94;
+  background:#fffaf9
+}
+.welcome-suggestions strong {
+  display:block;
+  color:#363c44;
+  font-size:13px;
+  font-weight:600;
+  margin:20px 0 5px
+}
+.welcome-suggestions span {
+  display:block;
+  color:#9399a3;
+  font-size:11px
+}
+.suggestion-arrow {
+  position:absolute;
+  right:17px;
+  top:22px;
+  color:#b8bec7
+}
+.welcome-topics {
+  display:flex;
+  gap:10px 15px;
+  justify-content:center;
+  flex-wrap:wrap;
+  max-width:700px;
+  margin:auto;
+  font-size:11px
+}
+.welcome-topics>span {
+  color:#929aa5
+}
+.welcome-topics button {
+  color:#5c6573;
+  text-decoration:underline;
+  text-underline-offset:4px;
+  text-decoration-color:#d2d7de
+}
+.welcome-language {
+  font-size:12px;
+  color:#9299a3;
+  margin-top:32px
+}
+.conversation-messages {
+  flex:1;
+  min-height:0;
+  overflow-y:auto;
+  padding:30px max(12px,calc((100% - 850px)/2));
+  scroll-behavior:smooth
+}
+.coach-message {
+  margin-bottom:18px;
+  padding:8px 16px;
+  line-height:1.85
+}
+.coach-message-user>div:first-child {
+  background:#f0f1f4!important;
+  color:#303640;
+  border-radius:14px 14px 4px 14px;
+  box-shadow:none;
+  max-width:85%;
+  font-size:15px;
+  padding:12px 18px
+}
+.coach-message-user>div:last-child {
+  display:none
+}
+.coach-message-assistant>div:first-child {
+  background:#fff1ee;
+  border:1px solid #ffdfd8;
+  color:#df4c40;
+  box-shadow:none;
+  border-radius:10px
+}
+.coach-message-assistant>div:first-child svg {
+  color:#df4c40
+}
+.coach-message-assistant>div:last-child {
+  max-width:calc(100% - 44px);
+  width:100%
+}
+.answer-surface {
+  background:transparent;
+  padding:8px 0 12px;
+  border:0;
+  box-shadow:none
+}
+.answer-surface>div:first-child {
+  font-size:15px;
+  line-height:1.9;
+  color:#434b58
+}
+.answer-surface>div:first-child li {
+  line-height:1.9
+}
+.answer-surface>div:last-child {
+  border-color:#eceff3;
+  padding-top:12px
+}
+.coach-message-assistant .text-xs.font-bold {
+  font-size:13px
+}
+.conversation-actions {
+  display:flex;
+  gap:7px;
+  flex-wrap:wrap;
+  width:min(100%,850px);
+  align-self:center;
+  padding:4px 28px 8px
+}
+.conversation-actions button {
+  font-size:11px;
+  border-radius:7px;
+  box-shadow:none;
+  padding:7px 9px
+}
+.conversation-composer {
+  width:min(100%,880px);
+  align-self:center;
+  padding:12px 30px 19px
+}
+.composer-field {
+  position:relative;
+  border:1px solid #dfe3e9;
+  border-radius:15px;
+  background:white;
+  box-shadow:0 4px 20px #1f293706
+}
+.composer-field:focus-within {
+  border-color:#d58c87;
+  box-shadow:0 0 0 3px #e8333008
+}
+.composer-field input {
+  border:0;
+  box-shadow:none!important;
+  outline:none!important;
+  min-height:64px;
+  padding:18px 65px 18px 20px;
+  border-radius:15px;
+  font-size:14px;
+  background:transparent
+}
+.composer-field button {
+  box-shadow:none;
+  border-radius:10px;
+  right:12px
+}
+.conversation-composer>p {
+  font-size:10px;
+  color:#989faa;
+  margin-top:10px
+}
+@media(max-width:1000px) {
+  .workspace-header {
+    padding:0 18px;
+    gap:15px
+  }
+  .account-name {
+    display:none
+  }
+  .coach-sidebar {
+    width:235px
+  }
+  .welcome-suggestions {
+    gap:8px
+  }
+  .welcome-suggestions button {
+    padding:16px 12px
+  }
+  .welcome-suggestions strong {
+    line-height:1.5
+  }
+  .conversation-toolbar {
+    padding:16px 22px
+  }
+}
+@media(max-width:700px) {
+  .workspace-header {
+    height:60px;
+    gap:10px;
+    padding:0 12px
+  }
+  .workspace-brand strong {
+    font-size:14px
+  }
+  .workspace-name,.workspace-divider {
+    display:none
+  }
+  .workspace-nav {
+    gap:0
+  }
+  .workspace-nav button {
+    font-size:11px;
+    padding:8px
+  }
+  .workspace-nav button svg {
+    display:none
+  }
+  .account-menu summary>svg {
+    display:none
+  }
+  .account-avatar {
+    width:30px;
+    height:30px
+  }
+  .coach-sidebar {
+    display:none;
+    position:absolute;
+    inset:63px auto 0 0;
+    z-index:20;
+    width:min(290px,85vw);
+    box-shadow:8px 10px 25px #171b2820
+  }
+  .coach-sidebar.is-open {
+    display:flex
+  }
+  .mobile-sidebar-toggle {
+    display:block;
+    padding:7px;
+    color:#697383
+  }
+  .conversation-toolbar {
+    gap:10px;
+    padding:10px 14px;
+    min-height:63px
+  }
+  .conversation-title strong {
+    font-size:12px
+  }
+  .conversation-title>span {
+    font-size:10px
+  }
+  .toolbar-new {
+    padding:7px
+  }
+  .toolbar-new span {
+    display:none
+  }
+  .save-status {
+    font-size:10px
+  }
+  .coach-welcome {
+    padding:30px 20px 20px
+  }
+  .welcome-mark {
+    margin-bottom:18px;
+    width:44px;
+    height:44px
+  }
+  .welcome-suggestions {
+    grid-template-columns:1fr;
+    margin-top:22px;
+    gap:9px
+  }
+  .welcome-suggestions button {
+    padding:16px 18px 16px 52px
+  }
+  .welcome-suggestions button>svg:first-child {
+    position:absolute;
+    top:21px;
+    left:18px;
+    width:18px
+  }
+  .welcome-suggestions strong {
+    margin:0 0 3px
+  }
+  .welcome-description {
+    font-size:13px;
+    margin-top:13px
+  }
+  .welcome-language {
+    margin-top:20px
+  }
+  .conversation-messages {
+    padding:18px 2px
+  }
+  .coach-message {
+    padding:5px 12px;
+    gap:9px
+  }
+  .answer-surface>div:first-child {
+    font-size:14px
+  }
+  .conversation-composer {
+    padding:8px 12px 12px
+  }
+  .conversation-actions {
+    padding:3px 12px 6px;
+    flex-wrap:nowrap;
+    overflow-x:auto
+  }
+  .conversation-actions button {
+    flex-shrink:0
+  }
+  .conversation-composer>p {
+    font-size:9px
+  }
+  .composer-field input {
+    min-height:56px;
+    font-size:13px
+  }
+  .coach-message-user>div:first-child {
+    font-size:14px
+  }
+  .welcome-topics {
+    gap:10px
+  }
+}
+@media(prefers-reduced-motion:reduce) {
+  *,*::before,*::after {
+    scroll-behavior:auto!important;
+    animation-duration:.01ms!important;
+    transition:none!important
+  }
+}
+.signin-layout {
+  min-height:100dvh;
+  display:grid;
+  grid-template-columns:minmax(320px,.9fr) 1.1fr;
+  background:white
+}
+.signin-story {
+  display:flex;
+  flex-direction:column;
+  justify-content:space-between;
+  background:#24272d;
+  color:white;
+  padding:48px clamp(32px,6vw,100px);
+  min-height:100dvh
+}
+.signin-wordmark {
+  font-size:18px;
+  font-weight:600;
+  letter-spacing:-.4px
+}
+.signin-story>div>p {
+  font-size:12px;
+  color:#b7becb;
+  margin-bottom:22px
+}
+.signin-story h2 {
+  font-size:clamp(32px,4vw,58px);
+  font-weight:500;
+  letter-spacing:-1.5px;
+  line-height:1.15;
+  margin-bottom:25px
+}
+.signin-story>div>span {
+  display:block;
+  max-width:340px;
+  color:#abb3c0;
+  font-size:15px;
+  line-height:1.8
+}
+.signin-story small {
+  color:#a0a9b7;
+  font-size:12px
+}
+.signin-form-area {
+  display:grid;
+  place-items:center;
+  padding:40px 24px
+}
+.signin-form {
+  max-width:370px;
+  width:100%
+}
+.signin-form h1 {
+  font-size:27px;
+  letter-spacing:-.7px;
+  margin-top:10px
+}
+.signin-form input {
+  border-color:#dfe3e9;
+  border-radius:8px;
+  padding:12px;
+  background:#fff
+}
+.signin-form>button {
+  min-height:44px
+}
+.signin-form>label {
+  color:#525b68
+}
+.signin-form button:not([type='button']) {
+  background:#e83330
+}
+.signin-form>div:first-child {
+  margin-bottom:28px
+}
+.signin-form .text-xs {
+  line-height:1.6
+}
+@media(max-width:700px) {
+  .signin-layout {
+    display:block
+  }
+  .signin-story {
+    min-height:auto;
+    padding:24px
+  }
+  .signin-story>div:not(.signin-wordmark),.signin-story small {
+    display:none
+  }
+  .signin-form-area {
+    padding:38px 24px
+  }
+  .signin-form h1 {
+    font-size:25px
+  }
+  .account-popover {
+    position:fixed;
+    top:60px;
+    right:10px
+  }
+}
+@media(max-width:700px) {
+  .answer-surface>div:last-child {
+    flex-wrap:wrap;
+    gap:10px
+  }
+  .answer-surface>div:last-child>div:first-child {
+    width:100%;
+    flex-wrap:wrap
+  }
+  .answer-surface>div:last-child>div:last-child {
+    margin-left:auto
+  }
+  .account-menu summary {
+    min-width:32px;
+    min-height:32px
+  }
+}
--- a/src/auth/AppShell.tsx
+++ b/src/auth/AppShell.tsx
@@ -1,15 +1,15 @@
 import { useState } from 'react';
+import { ArrowUpRight, ChevronDown, LogOut, MessageSquare, ShieldCheck, KeyRound } from 'lucide-react';
 import CommerceCoachPage from '../pages/CommerceCoachPage';
 import { useAuth,PasswordForm } from './AuthContext';
 import { UsersPanel } from './UsersPanel';
 export function AppShell(){
   const {user,logout}=useAuth();const [page,setPage]=useState('coach');const [error,setError]=useState('');
-  return <div className="flex h-dvh flex-col"><header className="flex flex-wrap items-center gap-3 border-b bg-white px-4 py-2 text-sm">
-    <strong className="mr-auto">{user.organization_name}</strong><span>{user.name} · {user.role}</span>
-    <button className="text-violet-700" onClick={()=>setPage('coach')}>Coach</button>
-    {user.role==='admin'&&<button className="text-violet-700" onClick={()=>setPage('users')}>Team access</button>}
-    <button className="text-violet-700" onClick={()=>setPage('password')}>Change password</button>
-    <button onClick={()=>void logout().catch(e=>setError(e.message))}>Sign out</button>
-  </header>{error&&<p role="alert" className="bg-red-50 p-2 text-red-700">{error}</p>}
+  return <div className="app-shell"><header className="workspace-header">
+    <a href="/" className="workspace-brand" aria-label="Commerce Coach home"><span className="brand-symbol"><ArrowUpRight size={19}/></span><strong>Commerce<span>Coach</span></strong></a>
+    <span className="workspace-divider"/><span className="workspace-name">{user.organization_name}</span>
+    <nav className="workspace-nav" aria-label="Workspace"><button aria-current={page==='coach'?'page':undefined} onClick={()=>setPage('coach')}><MessageSquare size={15}/>Coach</button>{user.role==='admin'&&<button aria-current={page==='users'?'page':undefined} onClick={()=>setPage('users')}><ShieldCheck size={15}/>Team access</button>}</nav>
+    <details className="account-menu"><summary><span className="account-avatar">{user.name.slice(0,1)}</span><span className="account-name">{user.name}<small>{user.role}</small></span><ChevronDown size={14}/></summary><div className="account-popover"><p>{user.email}</p><button onClick={event=>{setPage('password');event.currentTarget.closest('details')?.removeAttribute('open');}}><KeyRound size={15}/>Change password</button><button onClick={()=>void logout().catch(e=>setError(e.message))}><LogOut size={15}/>Sign out</button></div></details>
+  </header>{error&&<p role="alert" className="bg-red-50 p-3 text-red-700">{error}</p>}
   <main className="min-h-0 flex-1">{page==='users'&&user.role==='admin'?<UsersPanel/>:page==='password'?<div className="grid h-full place-items-center bg-gray-50 p-6"><PasswordForm done={()=>window.dispatchEvent(new Event('coach-session-expired'))}/></div>:<CommerceCoachPage/>}</main></div>;
 }
--- a/src/auth/AuthContext.tsx
+++ b/src/auth/AuthContext.tsx
@@ -17,7 +17,7 @@
     <label className="block text-sm">New password<input required minLength={12} maxLength={256} type="password" name="newPassword" autoComplete="new-password" className="mt-1 w-full rounded-lg border p-2" /></label>
     <label className="block text-sm">Confirm new password<input required type="password" name="confirm" autoComplete="new-password" className="mt-1 w-full rounded-lg border p-2" /></label>
     {error&&<p role="alert" className="text-sm text-red-700">{error}</p>}
-    <button disabled={busy} className="w-full rounded-lg bg-violet-600 p-2 text-white disabled:opacity-50">{busy?'Saving…':'Save password'}</button>
+    <button disabled={busy} className="w-full rounded-lg bg-red-600 p-2 text-white disabled:opacity-50">{busy?'Saving…':'Save password'}</button>
   </form>;
 }
 export function AuthProvider({children}:{children:ReactNode}) {
@@ -58,19 +58,19 @@
   },[]);
   const logout=async()=>{await api('/api/auth/logout',{});clear();};
   if(loading)return <div className="grid min-h-dvh place-items-center text-gray-500">Loading Commerce Coach…</div>;
-  if(user?.must_change_password)return <div className="grid min-h-dvh place-items-center bg-gray-50 p-6"><div><p className="mb-4 text-center text-sm text-gray-600">Welcome, {user.name}. Set your own password to continue.</p><PasswordForm done={clear}/><button className="mt-4 text-sm text-violet-700" onClick={()=>void logout().catch(e=>setError(e.message))}>Sign out</button>{error&&<p role="alert">{error}</p>}</div></div>;
-  if(!user)return <div className="grid min-h-dvh place-items-center bg-gray-50 p-6"><form className="w-full max-w-sm space-y-4 rounded-2xl border border-gray-200 bg-white p-7 shadow-sm" onSubmit={async event=>{
+  if(user?.must_change_password)return <div className="grid min-h-dvh place-items-center bg-gray-50 p-6"><div><p className="mb-4 text-center text-sm text-gray-600">Welcome, {user.name}. Set your own password to continue.</p><PasswordForm done={clear}/><button className="mt-4 text-sm text-red-700" onClick={()=>void logout().catch(e=>setError(e.message))}>Sign out</button>{error&&<p role="alert">{error}</p>}</div></div>;
+  if(!user)return <div className="signin-layout"><aside className="signin-story"><div className="signin-wordmark">Commerce Coach</div><div><p>Your everyday Commerce companion</p><h2>A clearer way<br/>to get things done.</h2><span>Understand your orders. Set up your store. Build your confidence, one conversation at a time.</span></div><small>Made for your team. Ready for বাংলা and English.</small></aside><div className="signin-form-area"><form className="signin-form space-y-4" onSubmit={async event=>{
     event.preventDefault();const data=new FormData(event.currentTarget);setBusy(true);setError('');
     try{await api('/api/auth/login',{organization:data.get('organization'),email:data.get('email'),password:data.get('password')});await refresh();}
     catch(e){setError((e as Error).message);}finally{setBusy(false);}
-  }}><div><div className="mb-2 text-sm font-semibold text-violet-600">Commerce Coach</div><h1 className="text-2xl font-bold">Sign in to your workspace</h1><p className="mt-2 text-sm text-gray-500">Use the account provided by your organization’s admin.</p></div>
+  }}><div><div className="mb-2 text-sm font-semibold text-red-600">Commerce Coach</div><h1 className="text-2xl font-bold">Sign in to your workspace</h1><p className="mt-2 text-sm text-gray-500">Use the account provided by your organization’s admin.</p></div>
     <label className="block text-sm">Organization<input required name="organization" value={organization} onChange={event=>setOrganization(event.target.value)} autoComplete="organization" placeholder="pathao" className="mt-1 w-full rounded-lg border p-2.5" /></label>
     <button type="button" disabled={busy||!googleEnabled} onClick={()=>void googleSignIn()} className="w-full rounded-lg border border-gray-300 bg-white p-2.5 font-semibold text-gray-800 hover:bg-gray-50 disabled:opacity-50">Continue with Google</button>
     {!googleEnabled&&<p className="text-xs text-gray-500">Google sign-in hasn’t been enabled by your administrator yet.</p>}
     <div className="text-center text-xs text-gray-400">or sign in with your password</div>
     <label className="block text-sm">Email<input required type="email" name="email" autoComplete="username" className="mt-1 w-full rounded-lg border p-2.5" /></label>
     <label className="block text-sm">Password<input required type="password" name="password" autoComplete="current-password" className="mt-1 w-full rounded-lg border p-2.5" /></label>
-    {error&&<p role="alert" className="text-sm text-red-700">{error}</p>}<button disabled={busy} className="w-full rounded-lg bg-violet-600 p-2.5 font-semibold text-white disabled:opacity-50">{busy?'Signing in…':'Sign in'}</button>
-  </form></div>;
+    {error&&<p role="alert" className="text-sm text-red-700">{error}</p>}<button disabled={busy} className="w-full rounded-lg bg-red-600 p-2.5 font-semibold text-white disabled:opacity-50">{busy?'Signing in…':'Sign in'}</button>
+  </form></div></div>;
   return <AuthContext.Provider value={{user,logout}}>{children}</AuthContext.Provider>;
 }
--- a/src/auth/UsersPanel.tsx
+++ b/src/auth/UsersPanel.tsx
@@ -18,7 +18,7 @@
       <label className="text-sm">Email<input required type="email" name="email" className="mt-1 w-full rounded border p-2" /></label>
       <label className="text-sm">Role<select name="role" className="mt-1 w-full rounded border p-2"><option value="member">Member</option><option value="reviewer">Reviewer</option><option value="admin">Admin</option></select></label>
       <label className="text-sm">Temporary password<input required minLength={12} maxLength={256} type="password" name="password" autoComplete="new-password" className="mt-1 w-full rounded border p-2" /></label>
-      <button disabled={busy} className="rounded bg-violet-600 px-4 py-2 text-white disabled:opacity-50">Create account</button>
+      <button disabled={busy} className="rounded bg-red-600 px-4 py-2 text-white disabled:opacity-50">Create account</button>
     </form>
     <div className="space-y-3">{users.map(user=><form key={`${user.id}-${user.role}-${user.active}`} className="flex flex-wrap items-end gap-3 rounded-xl border bg-white p-4" onSubmit={async event=>{
       event.preventDefault();const data=new FormData(event.currentTarget);setBusy(true);setError('');setNotice('');
--- a/src/pages/CommerceCoachPage.tsx
+++ b/src/pages/CommerceCoachPage.tsx
@@ -15,7 +15,7 @@
   Bot, Send, RotateCcw, BookOpen, Sparkles, AlertTriangle,
   ChevronRight, ChevronLeft, MessageSquare, Zap, BarChart3, Package,
   ShoppingCart, Store, Users, FileText, Truck, Lightbulb,
-  Copy, Check, ImageIcon, ExternalLink, Loader2, X, BrainCircuit
+  Copy, Check, ImageIcon, ExternalLink, Loader2, X, BrainCircuit, PanelLeft, Plus, Search, ArrowUpRight
 } from 'lucide-react';
 
 function uid(): string {
@@ -33,7 +33,7 @@
 const FEATURE_CARDS = [
   { icon: Package, label: 'Products', prompt: 'How do I create and manage products?', color: 'bg-blue-50 text-blue-600 border-blue-100' },
   { icon: ShoppingCart, label: 'Orders', prompt: 'How does order processing work?', color: 'bg-emerald-50 text-emerald-600 border-emerald-100' },
-  { icon: Store, label: 'Online Store', prompt: 'How do I set up my online store?', color: 'bg-purple-50 text-purple-600 border-purple-100' },
+  { icon: Store, label: 'Online Store', prompt: 'How do I set up my online store?', color: 'bg-red-50 text-red-600 border-red-100' },
   { icon: Truck, label: 'Delivery', prompt: 'How does delivery and fulfillment work?', color: 'bg-amber-50 text-amber-600 border-amber-100' },
   { icon: BarChart3, label: 'Analytics', prompt: 'How do I view my analytics and reports?', color: 'bg-rose-50 text-rose-600 border-rose-100' },
   { icon: Zap, label: 'Instant Checkout', prompt: 'How do I create instant checkout links?', color: 'bg-orange-50 text-orange-600 border-orange-100' },
@@ -56,7 +56,7 @@
 function TypingIndicator() {
   return (
     <div className="flex gap-3 px-6 py-3 justify-start">
-      <div className="w-8 h-8 rounded-xl bg-gradient-to-br from-violet-500 to-purple-600 flex items-center justify-center shrink-0 shadow-md">
+      <div className="w-8 h-8 rounded-xl bg-[#e83330] flex items-center justify-center shrink-0 shadow-md">
         <Bot size={16} className="text-white" />
       </div>
       <div className="flex items-center gap-1.5 bg-gray-100 rounded-2xl rounded-bl-md px-4 py-3">
@@ -68,45 +68,18 @@
 }
 
 function WelcomeScreen({ onSend }: { onSend: (text: string) => void }) {
-  return (
-    <div className="flex-1 flex flex-col items-center justify-center px-6 pb-8">
-      <div className="w-16 h-16 rounded-2xl bg-gradient-to-br from-violet-500 to-purple-600 flex items-center justify-center shadow-lg mb-4">
-        <Bot size={32} className="text-white" />
-      </div>
-      <h2 className="text-xl font-bold text-gray-900 mb-1">Commerce Coach</h2>
-      <p className="text-sm text-gray-500 mb-8 text-center max-w-md">
-        Your AI-powered guide for everything Pathao Commerce. Ask questions, learn features, troubleshoot issues, or test your knowledge.
-      </p>
-
-      <div className="grid grid-cols-3 gap-3 max-w-lg w-full mb-8">
-        {FEATURE_CARDS.map((card) => {
-          const Icon = card.icon;
-          return (
-            <button
-              key={card.label}
-              onClick={() => onSend(card.prompt)}
-              className={`flex flex-col items-center gap-2 p-4 rounded-xl border transition-all hover:scale-[1.02] hover:shadow-md cursor-pointer ${card.color}`}
-            >
-              <Icon size={20} />
-              <span className="text-xs font-semibold">{card.label}</span>
-            </button>
-          );
-        })}
-      </div>
-
-      <div className="flex flex-wrap gap-2 justify-center max-w-lg">
-        {['What is Pathao Commerce?', 'How do I sign up?', 'Guide me through setup'].map((q) => (
-          <button
-            key={q}
-            onClick={() => onSend(q)}
-            className="px-3 py-1.5 rounded-full border border-gray-200 text-xs font-medium text-gray-600 hover:bg-gray-50 hover:border-gray-300 transition-colors cursor-pointer"
-          >
-            {q}
-          </button>
-        ))}
-      </div>
-    </div>
-  );
+  return <div className="coach-welcome"><div className="welcome-mark"><ArrowUpRight size={28}/></div>
+    <p className="welcome-kicker">Your commerce workspace</p><h1>What can we work on?</h1>
+    <p className="welcome-description">From your first warehouse to your next order.<br/>Get clear steps, visual guides, and a little practice.</p>
+    <div className="welcome-suggestions">
+      {[
+        {icon:Package,title:'Set up a warehouse',description:'Your stock starts here',prompt:'ওয়্যারহাউস কীভাবে তৈরি করব?'},
+        {icon:ShoppingCart,title:'Understand your orders',description:'From checkout to delivery',prompt:'How does order processing work?'},
+        {icon:BookOpen,title:'Learn something new',description:'A guided Commerce lesson',prompt:'Train me on Commerce basics'},
+      ].map(({icon:Icon,title,description,prompt})=><button key={title} onClick={()=>onSend(prompt)}><Icon size={21}/><strong>{title}</strong><span>{description}</span><ArrowUpRight size={16} className="suggestion-arrow"/></button>)}
+    </div><div className="welcome-topics"><span>Explore</span>{FEATURE_CARDS.map(card=><button key={card.label} onClick={()=>onSend(card.prompt)}>{card.label}</button>)}</div>
+    <p className="welcome-language">বাংলা বা English—যেভাবে স্বচ্ছন্দ, সেভাবেই প্রশ্ন করুন।</p>
+  </div>;
 }
 
 function MessageBubble(props: { message: ChatMessage; themeColor: string; query?: string; onImageClick?: (images: { src: string; caption: string }[], index: number) => void; key?: string }) {
@@ -125,10 +98,10 @@
   // User message
   if (isUser) {
     return (
-      <div className="flex gap-3 px-6 py-2 justify-end">
+      <div className="coach-message coach-message-user flex gap-3 px-6 py-2 justify-end">
         <div
           className="max-w-[70%] px-4 py-2.5 text-sm leading-relaxed text-white rounded-2xl rounded-br-md shadow-md"
-          style={{ backgroundColor: themeColor }}
+          style={{ backgroundColor: getThemeColor(themeColor) }}
         >
           {renderMarkdown(message.content)}
         </div>
@@ -150,8 +123,8 @@
   }));
 
   return (
-    <div className="flex gap-3 px-6 py-2 justify-start">
-      <div className="w-8 h-8 rounded-xl bg-gradient-to-br from-violet-500 to-purple-600 flex items-center justify-center shrink-0 shadow-md">
+    <div className="coach-message coach-message-assistant flex gap-3 px-6 py-2 justify-start">
+      <div className="w-8 h-8 rounded-xl bg-[#e83330] flex items-center justify-center shrink-0 shadow-md">
         <Bot size={16} className="text-white" />
       </div>
       <div className="max-w-[88%] xl:max-w-[80%] space-y-2">
@@ -175,8 +148,8 @@
         )}
 
         {/* Main content */}
-        <div className="bg-white border border-gray-100 rounded-2xl rounded-bl-md px-5 py-4 shadow-sm hover:shadow-md transition-shadow">
-          <div className="text-sm text-gray-700 leading-relaxed prose prose-sm max-w-none prose-headings:font-bold prose-headings:text-gray-900 prose-p:my-1.5 prose-ul:my-1.5 prose-li:my-0.5 prose-code:bg-gray-100 prose-code:px-1.5 prose-code:py-0.5 prose-code:rounded prose-code:text-sm prose-strong:text-gray-900 prose-a:text-violet-600 prose-a:no-underline hover:prose-a:underline">
+        <div className="answer-surface">
+          <div className="text-sm text-gray-700 leading-relaxed prose prose-sm max-w-none prose-headings:font-bold prose-headings:text-gray-900 prose-p:my-1.5 prose-ul:my-1.5 prose-li:my-0.5 prose-code:bg-gray-100 prose-code:px-1.5 prose-code:py-0.5 prose-code:rounded prose-code:text-sm prose-strong:text-gray-900 prose-a:text-red-600 prose-a:no-underline hover:prose-a:underline">
             {renderMarkdown(message.content)}
           </div>
           <div className="flex items-center justify-between mt-3 pt-2 border-t border-gray-50">
@@ -212,7 +185,7 @@
           <section className="rounded-2xl border border-gray-200 bg-white p-3 shadow-sm" aria-label="Product Memo screenshots">
             <div className="mb-3 flex items-center justify-between gap-3 px-1">
               <div className="flex items-center gap-2 text-xs font-semibold text-gray-800">
-                <ImageIcon size={14} className="text-violet-600" />
+                <ImageIcon size={14} className="text-red-600" />
                 Visual guide
               </div>
               <span className="text-[10px] text-gray-500">
@@ -225,7 +198,7 @@
                   key={screenshot.src}
                   type="button"
                   onClick={() => onImageClick?.(previewImages, idx)}
-                  className="group w-[min(22rem,80vw)] shrink-0 snap-start overflow-hidden rounded-xl border border-gray-200 bg-white text-left transition-colors hover:border-violet-300 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-violet-400 focus-visible:ring-offset-2"
+                  className="group w-[min(22rem,80vw)] shrink-0 snap-start overflow-hidden rounded-xl border border-gray-200 bg-white text-left transition-colors hover:border-red-300 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-red-400 focus-visible:ring-offset-2"
                 >
                   <div className="aspect-[16/9] overflow-hidden bg-slate-50">
                     <img
@@ -239,7 +212,7 @@
                   </div>
                   <div className="flex min-h-14 items-start gap-2 border-t border-gray-100 px-3 py-2.5">
                     {screenshot.step && (
-                      <span className="flex h-5 min-w-5 items-center justify-center rounded-md bg-violet-100 px-1 text-[10px] font-bold text-violet-700">
+                      <span className="flex h-5 min-w-5 items-center justify-center rounded-md bg-red-100 px-1 text-[10px] font-bold text-red-700">
                         {screenshot.step}
                       </span>
                     )}
@@ -257,7 +230,7 @@
 
 export default function CommerceCoachPage() {
   const { user } = useAuth();
-  const themeColor = 'purple';
+  const themeColor = 'red';
   const geminiConfigured = isGeminiConfig();
   const openRouterConfigured = isOpenRouterConfig();
 
@@ -265,6 +238,9 @@
   const {messages,setMessages,state:conversationState,setState:setConversationState}=history;
   const [inputValue, setInputValue] = useState('');
   const [isTyping, setIsTyping] = useState(false);
+  const [sidebarOpen,setSidebarOpen]=useState(false);
+  const [historySearch,setHistorySearch]=useState('');
+  useEffect(()=>{const close=(event:globalThis.KeyboardEvent)=>{if(event.key==='Escape')setSidebarOpen(false);};window.addEventListener('keydown',close);return()=>window.removeEventListener('keydown',close);},[]);
   const [activeSection, setActiveSection] = useState('chat');
   const [previewGallery, setPreviewGallery] = useState<{ images: { src: string; caption: string }[]; index: number } | null>(null);
   const [zoom, setZoom] = useState(1);
@@ -343,7 +319,7 @@
 
   const handleReset = () => {
     if(isTyping||history.loading||history.unsaved||history.status==='Saving…')return;
-    history.fresh();setInputValue('');setActiveSection('chat');
+    history.fresh();setInputValue('');setActiveSection('chat');setSidebarOpen(false);
   };
 
   const quickActions = getQuickActions(conversationState);
@@ -363,20 +339,20 @@
   const showWelcome = messages.length <= 1;
 
   return (
-    <div className="flex h-full bg-gray-50 rounded-none overflow-hidden border border-gray-200">
+    <div className="coach-layout">
       {/* Sidebar */}
-      <div className="w-60 bg-white border-r border-gray-100 flex flex-col shrink-0">
-        <div className="p-4 border-b border-gray-100">
+      <div className={`coach-sidebar ${sidebarOpen?'is-open':''}`} id="coach-sidebar">
+        <div className="sidebar-heading">
           <div className="flex items-center gap-3">
-            <div className="w-10 h-10 rounded-xl bg-gradient-to-br from-violet-500 to-purple-600 flex items-center justify-center shadow-md">
+            <div className="sidebar-coach-icon">
               <Bot size={20} className="text-white" />
             </div>
             <div>
-              <div className="text-sm font-bold text-gray-900">Commerce Coach</div>
+              <div className="sidebar-title">Your coach</div>
               <div className="text-[10px] text-gray-500 flex items-center gap-1.5">
-                <span className="w-1.5 h-1.5 rounded-full bg-green-400 animate-pulse" />
+                <span className="w-1.5 h-1.5 rounded-full bg-green-400" />
                 {openRouterConfigured || geminiConfigured ? (aiProvider === 'openrouter' ? 'OpenRouter AI' : 'Gemini AI') : 'Knowledge Base'}
-                {modeLabel && <span className="text-violet-500 font-semibold">&middot; {modeLabel}</span>}
+                {modeLabel && <span className="text-red-500 font-semibold">&middot; {modeLabel}</span>}
               </div>
             </div>
           </div>
@@ -390,7 +366,7 @@
                   disabled={!openRouterConfigured}
                   className={`flex-1 text-[10px] font-medium py-1.5 rounded-md transition-all cursor-pointer ${
                     aiProvider === 'openrouter'
-                      ? 'bg-white text-violet-700 shadow-sm'
+                      ? 'bg-white text-red-700 shadow-sm'
                       : openRouterConfigured
                         ? 'text-gray-500 hover:text-gray-700'
                         : 'text-gray-300 cursor-not-allowed'
@@ -403,7 +379,7 @@
                   disabled={!geminiConfigured}
                   className={`flex-1 text-[10px] font-medium py-1.5 rounded-md transition-all cursor-pointer ${
                     aiProvider === 'gemini'
-                      ? 'bg-white text-violet-700 shadow-sm'
+                      ? 'bg-white text-red-700 shadow-sm'
                       : geminiConfigured
                         ? 'text-gray-500 hover:text-gray-700'
                         : 'text-gray-300 cursor-not-allowed'
@@ -416,7 +392,7 @@
           )}
         </div>
 
-        <div className="flex-1 p-3 space-y-1">
+        <div className="sidebar-modes">
           {SIDEBAR_ITEMS.filter(item => item.id !== 'learning' || user.role !== 'member').map((item) => {
             const Icon = item.icon;
             const isActive = activeSection === item.id;
@@ -424,20 +400,18 @@
               <button
                 key={item.id}
                 onClick={() => {
-                  setActiveSection(item.id);
+                  setActiveSection(item.id);setSidebarOpen(false);
                   if (item.id !== 'chat' && sectionPrompts[item.id]) {
                     handleSend(sectionPrompts[item.id]);
                   }
                 }}
-                className={`w-full flex items-center gap-3 px-3 py-2.5 rounded-xl text-left transition-all cursor-pointer ${
-                  isActive
-                    ? 'bg-violet-50 text-violet-700 border border-violet-100 shadow-sm'
-                    : 'text-gray-500 hover:bg-gray-50 hover:text-gray-700'
-                }`}
+                aria-current={isActive?'page':undefined}
+                className={`mode-button ${isActive?'is-active':''}`}
+
               >
-                <Icon size={16} className={isActive ? 'text-violet-500' : ''} />
+                <Icon size={16} className={isActive ? 'text-red-500' : ''} />
                 <div>
-                  <div className="text-xs font-semibold">{item.label}</div>
+                  <div className="text-sm font-medium">{item.label}</div>
                   <div className="text-[10px] text-gray-400">{item.desc}</div>
                 </div>
               </button>
@@ -445,24 +419,31 @@
           })}
         </div>
 
+        <div className="sidebar-history"><div className="history-heading"><h2>Conversations</h2><button title="New conversation" aria-label="New conversation" disabled={isTyping||history.loading||history.unsaved||history.status==='Saving…'} onClick={handleReset}><Plus size={17}/></button></div>
+          <label className="history-search"><Search size={14}/><input aria-label="Search conversations" placeholder="Find a conversation" value={historySearch} onChange={event=>setHistorySearch(event.target.value)}/></label>
+          <div className="history-list">{history.items.filter(item=>item.title.toLowerCase().includes(historySearch.toLowerCase())).map(item=><button key={item.id} className={history.id===item.id?'is-active':''} aria-current={history.id===item.id?'true':undefined} disabled={isTyping||history.loading||history.unsaved||history.status==='Saving…'} onClick={()=>{void history.open(item.id);setActiveSection('chat');setSidebarOpen(false);}}><span>{item.title}</span><small>{new Date(item.updated_at).toLocaleDateString(undefined,{month:'short',day:'numeric'})} · {new Date(item.updated_at).toLocaleTimeString(undefined,{hour:'2-digit',minute:'2-digit'})}</small></button>)}
+          {!history.items.length&&<p>Your saved chats will appear here.</p>}
+          {history.items.length>0&&!history.items.some(item=>item.title.toLowerCase().includes(historySearch.toLowerCase()))&&<p>No matching conversations loaded.</p>}
+          {history.hasMore&&<button onClick={()=>void history.loadMore().catch(()=>{})}>Load older conversations</button>}</div>
+        </div>
         {/* Score badge */}
         {conversationState.quizScore.total > 0 && (
-          <div className="mx-3 mb-2 p-3 bg-gradient-to-r from-violet-50 to-purple-50 rounded-xl border border-violet-100">
-            <div className="text-[10px] text-violet-600 font-semibold uppercase tracking-wide mb-1">Quiz Score</div>
-            <div className="text-lg font-bold text-violet-700">
+          <div className="mx-3 mb-2 p-3 bg-gradient-to-r from-red-50 to-red-50 rounded-xl border border-red-100">
+            <div className="text-[10px] text-red-600 font-semibold uppercase tracking-wide mb-1">Quiz Score</div>
+            <div className="text-lg font-bold text-red-700">
               {conversationState.quizScore.correct}/{conversationState.quizScore.total}
-              <span className="text-xs font-normal text-violet-500 ml-1">
+              <span className="text-xs font-normal text-red-500 ml-1">
                 ({Math.round((conversationState.quizScore.correct / conversationState.quizScore.total) * 100)}%)
               </span>
             </div>
           </div>
         )}
 
-        <div className="p-3 border-t border-gray-100 space-y-2">
+        <div className="sidebar-footer">
           <button
             disabled={isTyping||history.loading||history.unsaved||history.status==='Saving…'}
             onClick={handleReset}
-            className="w-full flex items-center justify-center gap-2 px-3 py-2 rounded-xl border border-gray-200 text-xs font-semibold text-gray-500 hover:bg-gray-50 hover:text-gray-700 transition-colors cursor-pointer"
+            className="new-conversation-button"
           >
             <RotateCcw size={13} />
             New Conversation
@@ -478,16 +459,12 @@
       </div>
 
       {/* Chat Area */}
-      <div className="flex-1 flex flex-col min-w-0 bg-gray-50">
-        <div className="flex flex-wrap items-center gap-3 border-b border-gray-200 bg-white px-4 py-3">
-          <label className="text-xs text-gray-600" htmlFor="conversation-history">Your conversations</label>
-          <select id="conversation-history" className="min-w-0 max-w-64 rounded-lg border border-gray-200 p-2 text-xs" value={history.id} disabled={isTyping||history.loading||history.unsaved||history.status==='Saving…'} onChange={event=>{if(event.target.value){void history.open(event.target.value);setActiveSection('chat');}}}>
-            <option value="">New conversation</option>
-            {history.items.map(item=><option key={item.id} value={item.id}>{item.title}</option>)}
-          </select>
-          {history.hasMore&&<button className="text-xs text-violet-700" onClick={()=>void history.loadMore().catch(()=>{})}>Load older conversations</button>}
-          <button className="text-xs text-violet-700" disabled={isTyping||history.loading||history.unsaved||history.status==='Saving…'} onClick={handleReset}>New chat</button>
-          <span role="status" className="text-xs text-gray-500">{history.loading?'Loading history…':history.status}</span>
+      <div className="coach-main">
+        <div className="conversation-toolbar">
+          <button className="mobile-sidebar-toggle" aria-label="Toggle navigation" aria-expanded={sidebarOpen} aria-controls="coach-sidebar" onClick={()=>setSidebarOpen(!sidebarOpen)}><PanelLeft size={19}/></button>
+          <div className="conversation-title"><strong>{activeSection==='learning'?'Review corrections':history.items.find(item=>item.id===history.id)?.title || 'New conversation'}</strong><span>{modeLabel || 'Ask, learn, and put it into practice'}</span></div>
+          <span role="status" className="save-status">{history.loading?'Loading…':history.status==='Saved'?<><Check size={13}/>Saved</>:history.status}</span>
+          <button className="toolbar-new" title="New chat" aria-label="New chat" disabled={isTyping||history.loading||history.unsaved||history.status==='Saving…'} onClick={handleReset}><Plus size={17}/><span>New chat</span></button>
           {history.error&&<div role="alert" className="w-full text-xs text-red-700">{history.error} {history.unsaved&&<button disabled={isTyping} className="ml-2 underline" onClick={()=>void handleRetrySave()}>Retry saving</button>}</div>}
           {messages.length>=199&&<p className="w-full text-xs text-gray-500">This conversation is full. Start a new chat to continue.</p>}
         </div>
@@ -499,7 +476,7 @@
         {showWelcome ? (
           <WelcomeScreen onSend={handleSend} />
         ) : (
-          <div className="flex-1 overflow-y-auto py-4 scroll-smooth">
+          <div className="conversation-messages">
             {messages.slice(1).map((msg) => (
               <MessageBubble key={msg.id} message={msg} query={findFeedbackQuery(messages, msg.id)} themeColor={themeColor} onImageClick={(images, index) => { setPreviewGallery({ images, index }); setZoom(1); }} />
             ))}
@@ -510,7 +487,7 @@
 
         {/* Quick Actions */}
         {!isTyping && !showWelcome && quickActions.length > 0 && (
-          <div className="px-6 pb-2 flex flex-wrap gap-2">
+          <div className="conversation-actions">
             {quickActions.map((action) => (
               <button
                 key={action}
@@ -525,19 +502,21 @@
         )}
 
         {/* Input */}
-        <div className="px-6 pb-4 pt-2">
-          <div className="relative">
+        <div className="conversation-composer">
+          <div className="composer-field">
             <input
               ref={inputRef}
               type="text"
               value={inputValue}
               onChange={(e) => setInputValue(e.target.value)}
               onKeyDown={handleKeyDown}
-              placeholder={isTyping ? 'Coach is thinking...' : 'Ask about Commerce features, training, troubleshooting...'}
+              aria-label="Message Commerce Coach"
+              placeholder={isTyping ? 'Coach is thinking…' : 'Ask your coach… বাংলা বা English'}
               disabled={isTyping}
-              className="w-full bg-white border border-gray-200 rounded-2xl px-5 py-3.5 pr-14 text-sm text-gray-700 placeholder-gray-400 outline-none focus:border-violet-300 focus:ring-2 focus:ring-violet-100 transition-all disabled:opacity-50 shadow-sm"
+              className="w-full bg-white border border-gray-200 rounded-2xl px-5 py-3.5 pr-14 text-sm text-gray-700 placeholder-gray-400 outline-none focus:border-red-300 focus:ring-2 focus:ring-red-100 transition-all disabled:opacity-50 shadow-sm"
             />
             <button
+              aria-label="Send message"
               onClick={() => handleSend()}
               disabled={!inputValue.trim() || isTyping || history.loading || history.unsaved || history.status==='Saving…' || messages.length>=199}
               className="absolute right-2 top-1/2 -translate-y-1/2 w-10 h-10 flex items-center justify-center rounded-xl text-white disabled:opacity-30 disabled:cursor-not-allowed transition-all hover:scale-105 active:scale-95 cursor-pointer shadow-md"
@@ -547,7 +526,7 @@
             </button>
           </div>
           <p className="text-center text-[10px] text-gray-400 mt-2">
-            Powered by Pathao AI
+            Guides grounded in Commerce documentation. Check important details with your team.
           </p>
         </div>
           </>
